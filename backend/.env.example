# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tms_db
DB_USER=tms_user
DB_PASSWORD=tms_pass_2024

# Server
PORT=3001
NODE_ENV=development

# JWT
JWT_SECRET=tms_super_secret_jwt_key_2024
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# OCR
OCR_UPLOAD_PATH=./uploads
MAX_FILE_SIZE_MB=10
